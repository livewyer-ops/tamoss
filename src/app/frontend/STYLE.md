# Frontend Style Tokens

Use `lw-ink-*` for common TAMOSS text, border, muted, and light-surface roles.
Use `tams-*` for brand/action accents.

Raw Tailwind gray utilities remain acceptable for intentionally neutral browser
controls, third-party examples, and dark code/API payload panels where `lw-ink-*`
would reduce contrast or change the visual role.
