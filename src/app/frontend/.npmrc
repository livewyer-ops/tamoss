# Lifecycle scripts are the execution vector for essentially every npm
# supply-chain compromise: a malicious postinstall runs on the build machine
# with registry and CI credentials in scope, before any test or scan sees the
# package. No dependency in this tree needs them, so refuse them everywhere
# rather than per call site.
ignore-scripts=true
