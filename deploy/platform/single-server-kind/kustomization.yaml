apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Kind profile variants intentionally reuse the local platform prerequisites.
# Profile-specific behavior is expressed by the Tamoss instance overlay.
resources:
  - ../local-kind
