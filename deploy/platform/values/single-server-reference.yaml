certManager:
  enabled: true

cnpg:
  enabled: true

rustfsOperator:
  enabled: true

authentik:
  enabled: false
  ingress:
    host: auth.tamoss.example.com
    tlsSecretName: authentik-public-tls

traefik:
  enabled: true
  replicas: 2
  service:
    type: LoadBalancer

tls:
  mode: public
  issuerName: tamoss-public
  acme:
    email: ops@example.com
