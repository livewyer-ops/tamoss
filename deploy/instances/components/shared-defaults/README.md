# Shared Instance Defaults

This component holds defaults that all built-in TAMOSS instance overlays inherit.
Keep profile-specific values in each profile directory, and move only values that
must stay consistent across every built-in instance here.
