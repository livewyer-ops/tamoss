# https://taskfile.dev
version: "3"

set: [errexit, nounset, pipefail]

tasks:
  ingest:
    requires:
      vars: [VIDEO]
    cmds:
      - bash src/scripts/ingest_video.sh "{{.VIDEO}}" '{{default "" .LABEL}}' '{{default "" .DESCRIPTION}}' '{{default "3" .DURATION}}'
