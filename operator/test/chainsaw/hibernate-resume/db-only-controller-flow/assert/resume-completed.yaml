apiVersion: tamoss.livewyer.io/v1alpha1
kind: TamossResume
metadata:
  name: resume
  namespace: ($namespace)
(status.phase): Completed
(status.reason): TamossReady
(status.artifact.cleanup.phase): Completed
(status.artifact.cleanup.reason): ArtifactCleanupComplete
"(status.artifact.cleanup.objectsDeleted > `0`)": true
(status.artifact.cleanup.completedAt != null): true
---
apiVersion: tamoss.livewyer.io/v1alpha1
kind: Tamoss
metadata:
  name: tamoss-restored
  namespace: ($namespace)
status:
  lifecycle:
    phase: Running
    reason: TamossReady
    lastResumeRef:
      name: resume
      kind: TamossResume
