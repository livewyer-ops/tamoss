apiVersion: tamoss.livewyer.io/v1alpha1
kind: TamossHibernate
metadata:
  annotations:
    confirmation.tamoss.livewyer.io/deletion: "true"
  name: snapshot
spec:
  tamossRef:
    name: tamoss
  destination:
    storageBackendRef:
      name: tamoss-hibernate
    prefix: hibernations/tamoss
