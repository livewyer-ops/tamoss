apiVersion: apps/v1
kind: Deployment
metadata:
  name: operator-controller-manager
  namespace: tamoss-system
(status.conditions[?type == 'Available'] | [0].status): "True"
(status.observedGeneration == metadata.generation): true
(status.updatedReplicas == spec.replicas): true
(status.availableReplicas == spec.replicas): true
(status.replicas == spec.replicas): true
---
apiVersion: v1
kind: Endpoints
metadata:
  name: operator-webhook-service
  namespace: tamoss-system
(subsets[0].addresses[0].ip != ''): true
