apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: api-pod-uses-cnpg-secret
  labels:
    test.tamoss.io/target: kind
    test.tamoss.io/tier: standard
    test.tamoss.io/domain: db
    test.tamoss.io/lifecycle: ephemeral
    test.tamoss.io/provider: cnpg
    test.tamoss.io/profile: none
spec:
  steps:
    - name: apply ready cnpg instance
      try:
        - apply:
            file: ../../fixtures/cnpg-crd.yaml
        - apply:
            file: ../../fixtures/external-backend-secrets.yaml
        - apply:
            file: ../../fixtures/schema-state.yaml
        - apply:
            file: ../../fixtures/tamoss-cnpg.yaml
        - assert:
            file: assert/cnpg-cluster-crd-established.yaml
            timeout: 120s
        - assert:
            file: assert/tamoss-db-cluster.yaml
            timeout: 120s
        - apply:
            file: ../../fixtures/cnpg-secrets.yaml
        - patch:
            resource:
              apiVersion: postgresql.cnpg.io/v1
              kind: Cluster
              metadata:
                name: tamoss-db
                namespace: ($namespace)
              status:
                conditions:
                  - type: Ready
                    status: "True"
                    reason: ClusterReady
                    message: Cluster is ready
            subresource: status
            timeout: 60s
        - apply:
            file: ../../fixtures/cnpg-simulated-pod.yaml
            bindings:
              - name: cluster
                value: (x_k8s_get($client, 'postgresql.cnpg.io/v1', 'Cluster', $namespace, 'tamoss-db'))
              - name: ordinal
                value: "1"
        - apply:
            file: ../../fixtures/cnpg-simulated-pod.yaml
            bindings:
              - name: cluster
                value: (x_k8s_get($client, 'postgresql.cnpg.io/v1', 'Cluster', $namespace, 'tamoss-db'))
              - name: ordinal
                value: "2"
        - apply:
            file: ../../fixtures/cnpg-simulated-pod.yaml
            bindings:
              - name: cluster
                value: (x_k8s_get($client, 'postgresql.cnpg.io/v1', 'Cluster', $namespace, 'tamoss-db'))
              - name: ordinal
                value: "3"
        - assert:
            file: assert/api-deployment-uses-cnpg-secret.yaml
            timeout: 120s
