apiVersion: apps/v1
kind: Deployment
metadata:
  name: operator-controller-manager
  namespace: tamoss-system
spec:
  template:
    spec:
      containers:
        - name: manager
          env:
            - name: TAMOSS_DEPENDENCY_DISCOVERY_INTERVAL
              value: 5s
