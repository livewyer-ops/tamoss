---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: flowprofiles.tamoss.livewyer.io
spec:
  group: tamoss.livewyer.io
  names:
    kind: FlowProfile
    listKind: FlowProfileList
    plural: flowprofiles
    shortNames:
    - fp
    singular: flowprofile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tamossRef.name
      name: Tamoss
      type: string
    - jsonPath: .status.profileID
      name: Profile ID
      type: string
    - jsonPath: .status.resolved.format
      name: Format
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FlowProfile declaratively registers an immutable TAMS Flow Profile.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              description:
                type: string
              flowMetadata:
                description: |-
                  FlowMetadata is copied to the TAMS flow_metadata object. The operator
                  preserves extensions and the TAMOSS registration command applies the full
                  TAMS contract validation before writing anything.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              id:
                description: |-
                  ID is the immutable TAMS Profile UUID. Empty derives a deterministic UUID
                  from the FlowProfile namespace and name.
                pattern: ^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$
                type: string
              label:
                type: string
              tags:
                additionalProperties:
                  x-kubernetes-preserve-unknown-fields: true
                description: Tags use the TAMS string-or-string-array value union.
                type: object
              tamossRef:
                description: TamossRef identifies the same-namespace Tamoss instance
                  receiving the Profile.
                properties:
                  name:
                    minLength: 1
                    type: string
                type: object
            required:
            - flowMetadata
            - tamossRef
            type: object
            x-kubernetes-validations:
            - message: spec.id is immutable
              rule: has(self.id) == has(oldSelf.id) && (!has(self.id) || self.id ==
                oldSelf.id)
            - message: spec.tamossRef is immutable
              rule: self.tamossRef == oldSelf.tamossRef
            - message: spec.label is immutable
              rule: has(self.label) == has(oldSelf.label) && (!has(self.label) ||
                self.label == oldSelf.label)
            - message: spec.description is immutable
              rule: has(self.description) == has(oldSelf.description) && (!has(self.description)
                || self.description == oldSelf.description)
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                format: int64
                type: integer
              phase:
                enum:
                - Pending
                - Progressing
                - Ready
                - Degraded
                - Deleting
                type: string
              profileID:
                type: string
              resolved:
                properties:
                  codec:
                    type: string
                  format:
                    type: string
                  tamossName:
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
