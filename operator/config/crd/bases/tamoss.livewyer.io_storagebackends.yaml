---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: storagebackends.tamoss.livewyer.io
spec:
  group: tamoss.livewyer.io
  names:
    kind: StorageBackend
    listKind: StorageBackendList
    plural: storagebackends
    shortNames:
    - sb
    singular: storagebackend
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tamossRef.name
      name: Tamoss
      type: string
    - jsonPath: .spec.provider
      name: Provider
      type: string
    - jsonPath: .spec.bucketName
      name: Bucket
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StorageBackend is the Schema for declarative TAMS storage backend
          registration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              bucketName:
                minLength: 1
                type: string
              credentials:
                properties:
                  existingSecret:
                    type: string
                  secretKeys:
                    properties:
                      accessKey:
                        type: string
                      password:
                        type: string
                      secretKey:
                        type: string
                      username:
                        type: string
                    type: object
                type: object
              defaultStorage:
                default: false
                type: boolean
              endpoint:
                properties:
                  default:
                    properties:
                      url:
                        minLength: 1
                        type: string
                    type: object
                  public:
                    properties:
                      url:
                        minLength: 1
                        type: string
                    type: object
                type: object
              hibernate:
                properties:
                  retention:
                    properties:
                      mode:
                        default: Retain
                        description: |-
                          Mode controls operator-managed deletion of completed hibernation artifacts.
                          Retain keeps artifacts indefinitely. DeleteAfterResume removes the source
                          artifact after a successful resume. TTL removes it once the restored
                          database has been ready for ttlSecondsAfterResume.
                        enum:
                        - Retain
                        - DeleteAfterResume
                        - TTL
                        type: string
                      ttlSecondsAfterResume:
                        description: TTLSecondsAfterResume is required when mode is
                          TTL.
                        format: int64
                        minimum: 1
                        type: integer
                    type: object
                type: object
              id:
                description: |-
                  ID is the BBC TAMS storage backend UUID advertised to clients.
                  Empty lets the operator derive a deterministic UUID from namespace/name.
                format: uuid
                type: string
              label:
                minLength: 1
                type: string
              provider:
                default: rustfs
                description: Provider selects the storage implementation reconciled
                  by this resource.
                enum:
                - rustfs
                - external-s3
                type: string
              region:
                default: us-east-1
                minLength: 1
                type: string
              storeProduct:
                default: s3
                minLength: 1
                type: string
              storeType:
                default: http_object_store
                minLength: 1
                type: string
              tamossRef:
                description: TamossRef identifies the Tamoss instance that owns the
                  TAMS database registration.
                properties:
                  name:
                    minLength: 1
                    type: string
                type: object
              usage:
                default: media
                description: |-
                  Usage declares how this object store is consumed. Media backends are
                  registered in the TAMS database and exposed to the application runtime.
                  Hibernate backends are reserved for DB hibernation artifacts and are not
                  registered as media storage.
                enum:
                - media
                - hibernate
                type: string
            type: object
            x-kubernetes-validations:
            - message: spec.id is immutable
              rule: has(self.id) == has(oldSelf.id) && (!has(self.id) || self.id ==
                oldSelf.id)
            - message: spec.provider is immutable
              rule: self.provider == oldSelf.provider
            - message: spec.bucketName is immutable
              rule: has(self.bucketName) == has(oldSelf.bucketName) && (!has(self.bucketName)
                || self.bucketName == oldSelf.bucketName)
            - message: spec.tamossRef.name is required
              rule: has(self.tamossRef) && has(self.tamossRef.name) && self.tamossRef.name.size()
                > 0
            - message: spec.tamossRef.name is immutable
              rule: self.tamossRef.name == oldSelf.tamossRef.name
            - message: spec.usage is immutable
              rule: has(self.usage) == has(oldSelf.usage) && (!has(self.usage) ||
                self.usage == oldSelf.usage)
            - message: spec.endpoint.default.url is required when spec.provider is
                external-s3
              rule: self.provider != 'external-s3' || (has(self.endpoint) && has(self.endpoint.default)
                && has(self.endpoint.default.url) && self.endpoint.default.url.size()
                > 0)
            - message: hibernate StorageBackends must use provider external-s3
              rule: self.usage != 'hibernate' || self.provider == 'external-s3'
            - message: hibernate.retention.ttlSecondsAfterResume is required when
                retention mode is TTL
              rule: '!has(self.hibernate) || !has(self.hibernate.retention) || !has(self.hibernate.retention.mode)
                || self.hibernate.retention.mode != ''TTL'' || (has(self.hibernate.retention.ttlSecondsAfterResume)
                && self.hibernate.retention.ttlSecondsAfterResume > 0)'
          status:
            properties:
              backendID:
                type: string
              bucketName:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              phase:
                enum:
                - Pending
                - Progressing
                - Ready
                - Degraded
                type: string
              resolved:
                properties:
                  backendID:
                    type: string
                  bucketName:
                    type: string
                  credentialsSecret:
                    type: string
                  endpointURL:
                    type: string
                  provider:
                    type: string
                  publicEndpointURL:
                    type: string
                  usage:
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
